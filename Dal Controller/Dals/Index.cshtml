@model IEnumerable<HospitalApp.Models.Dal>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DalName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DalName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DalId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DalId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DalId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
