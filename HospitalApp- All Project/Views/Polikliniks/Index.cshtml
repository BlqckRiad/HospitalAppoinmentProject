@model IEnumerable<HospitalApp.Models.Poliklinik>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PolName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PolName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PolId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PolId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PolId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
